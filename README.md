
## Available Scripts

to run react app => 
In the project directory, you can run:

### `npm install`
### `npm start`


to run node app => 
In the project directory, Go to "node-pg-log-api" you can run:

### `nodemon`
